<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name="ru.geekbrains.weatherapplication.BaseApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_weather_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_weather_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppCustomLightTheme">
        <activity android:name="ru.geekbrains.weatherapplication.SettingsActivity" />

        <activity android:name="ru.geekbrains.weatherapplication.gps.BaseAppActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>


    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission. ACCESS_COARSE_LOCATION" />
</manifest>